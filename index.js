//const {something} = require('./somethingElse'); <== just a formula for 

const prompt = require('prompt-sync')();

console.log('Hello, and welcome to Embezzlers (now Audited)! What would you like to do today?');

const mainMenuItem = prompt();

console